name: flutter_gl_sample_windows
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0-42.2.beta <4.0.0'
dependencies:
  flutter:
    sdk: flutter

  flutter_gl:
    git: 
      url: https://github.com/wasabia/flutter_gl
      ref: main
      path: flutter_gl/
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
